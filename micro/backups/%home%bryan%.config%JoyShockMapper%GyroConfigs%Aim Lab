# Aim Lab by snakedye

# First, reset to defaults so we don't have to set values we don't care about
RESET_MAPPINGS

# Calibrate. Flick stick relies on good calibration; gyro and stick sens make more sens with it, too
REAL_WORLD_CALIBRATION = 37.761

IN_GAME_SENS = 2

# Button mapping

LLEFT = A
LRIGHT = D
LUP = W
LDOWN = S
N = V # Toggle F3P
E = X # Detection
S = SPACE # Jump
W = R # Reload
L = Q # Lean Left
R = E # Lean Right
ZL = RMOUSE # Aim
ZR = LMOUSE # Shoot
L3 = LSHIFT # Sprint
R3 = C # Crouch
+ = ESC # Pause/Menu
- = F6

# Include mouse settings
GyroConfigs/_3Dmouse.txt
