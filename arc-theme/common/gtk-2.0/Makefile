# Makefile.in generated by automake 1.16.2 from Makefile.am.
# common/gtk-2.0/Makefile.  Generated from Makefile.in by configure.

# Copyright (C) 1994-2020 Free Software Foundation, Inc.

# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.



am__is_gnu_make = { \
  if test -z '$(MAKELEVEL)'; then \
    false; \
  elif test -n '$(MAKE_HOST)'; then \
    true; \
  elif test -n '$(MAKE_VERSION)' && test -n '$(CURDIR)'; then \
    true; \
  else \
    false; \
  fi; \
}
am__make_running_with_option = \
  case $${target_option-} in \
      ?) ;; \
      *) echo "am__make_running_with_option: internal error: invalid" \
              "target option '$${target_option-}' specified" >&2; \
         exit 1;; \
  esac; \
  has_opt=no; \
  sane_makeflags=$$MAKEFLAGS; \
  if $(am__is_gnu_make); then \
    sane_makeflags=$$MFLAGS; \
  else \
    case $$MAKEFLAGS in \
      *\\[\ \	]*) \
        bs=\\; \
        sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
    esac; \
  fi; \
  skip_next=no; \
  strip_trailopt () \
  { \
    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
  }; \
  for flg in $$sane_makeflags; do \
    test $$skip_next = yes && { skip_next=no; continue; }; \
    case $$flg in \
      *=*|--*) continue;; \
        -*I) strip_trailopt 'I'; skip_next=yes;; \
      -*I?*) strip_trailopt 'I';; \
        -*O) strip_trailopt 'O'; skip_next=yes;; \
      -*O?*) strip_trailopt 'O';; \
        -*l) strip_trailopt 'l'; skip_next=yes;; \
      -*l?*) strip_trailopt 'l';; \
      -[dEDm]) skip_next=yes;; \
      -[JT]) skip_next=yes;; \
    esac; \
    case $$flg in \
      *$$target_option*) has_opt=yes; break;; \
    esac; \
  done; \
  test $$has_opt = yes
am__make_dryrun = (target_option=n; $(am__make_running_with_option))
am__make_keepgoing = (target_option=k; $(am__make_running_with_option))
pkgdatadir = $(datadir)/arc-theme
pkgincludedir = $(includedir)/arc-theme
pkglibdir = $(libdir)/arc-theme
pkglibexecdir = $(libexecdir)/arc-theme
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
subdir = common/gtk-2.0
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/arc-cinnamon.m4 \
	$(top_srcdir)/m4/arc-enable.m4 \
	$(top_srcdir)/m4/arc-gnome-shell.m4 \
	$(top_srcdir)/m4/arc-gtk3.m4 $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
DIST_COMMON = $(srcdir)/Makefile.am $(am__DIST_COMMON)
mkinstalldirs = $(install_sh) -d
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_P = $(am__v_P_$(V))
am__v_P_ = $(am__v_P_$(AM_DEFAULT_VERBOSITY))
am__v_P_0 = false
am__v_P_1 = :
AM_V_GEN = $(am__v_GEN_$(V))
am__v_GEN_ = $(am__v_GEN_$(AM_DEFAULT_VERBOSITY))
am__v_GEN_0 = @echo "  GEN     " $@;
am__v_GEN_1 = 
AM_V_at = $(am__v_at_$(V))
am__v_at_ = $(am__v_at_$(AM_DEFAULT_VERBOSITY))
am__v_at_0 = @
am__v_at_1 = 
SOURCES =
DIST_SOURCES =
am__can_run_installinfo = \
  case $$AM_UPDATE_INFO_DIR in \
    n|no|NO) false;; \
    *) (install-info --version) >/dev/null 2>&1;; \
  esac
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__installdirs = "$(DESTDIR)$(gtk2themedir)" \
	"$(DESTDIR)$(gtk2themeassetsdir)" \
	"$(DESTDIR)$(gtk2themedarkdir)" \
	"$(DESTDIR)$(gtk2themedarkassetsdir)" \
	"$(DESTDIR)$(gtk2themedarkerdir)" \
	"$(DESTDIR)$(gtk2themedarkerassetsdir)" \
	"$(DESTDIR)$(gtk2themelighterdir)" \
	"$(DESTDIR)$(gtk2themelighterassetsdir)" \
	"$(DESTDIR)$(gtk2themedir)" "$(DESTDIR)$(gtk2themedarkdir)" \
	"$(DESTDIR)$(gtk2themedarkerdir)" \
	"$(DESTDIR)$(gtk2themelighterdir)"
DATA = $(gtk2theme_DATA) $(gtk2themeassets_DATA) $(gtk2themedark_DATA) \
	$(gtk2themedarkassets_DATA) $(gtk2themedarker_DATA) \
	$(gtk2themedarkerassets_DATA) $(gtk2themelighter_DATA) \
	$(gtk2themelighterassets_DATA) $(nobase_gtk2theme_DATA) \
	$(nobase_gtk2themedark_DATA) $(nobase_gtk2themedarker_DATA) \
	$(nobase_gtk2themelighter_DATA)
am__tagged_files = $(HEADERS) $(SOURCES) $(TAGS_FILES) $(LISP)
am__DIST_COMMON = $(srcdir)/Makefile.in
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)

ACLOCAL = ${SHELL} /home/bryan/arc-theme/build-aux/missing aclocal-1.16
AMTAR = $${TAR-tar}
AM_DEFAULT_VERBOSITY = 1
AUTOCONF = ${SHELL} /home/bryan/arc-theme/build-aux/missing autoconf
AUTOHEADER = ${SHELL} /home/bryan/arc-theme/build-aux/missing autoheader
AUTOMAKE = ${SHELL} /home/bryan/arc-theme/build-aux/missing automake-1.16
AWK = gawk
CINNAMON_FOUND = 
CINNAMON_VERSION = 
CYGPATH_W = echo
DEFS = -DPACKAGE_NAME=\"arc-theme\" -DPACKAGE_TARNAME=\"arc-theme\" -DPACKAGE_VERSION=\"20200819\" -DPACKAGE_STRING=\"arc-theme\ 20200819\" -DPACKAGE_BUGREPORT=\"https://github.com/jnsh/arc-theme/issues\" -DPACKAGE_URL=\"https://github.com/jnsh/arc-theme\" -DPACKAGE=\"arc-theme\" -DVERSION=\"20200819\"
ECHO_C = 
ECHO_N = -n
ECHO_T = 
ENABLE_CINNAMON = no
ENABLE_DARK = yes
ENABLE_DARKER = yes
ENABLE_GNOME_SHELL = no
ENABLE_GTK2 = yes
ENABLE_GTK3 = yes
ENABLE_LIGHT = yes
ENABLE_LIGHTER = yes
ENABLE_METACITY = yes
ENABLE_PLANK = yes
ENABLE_TRANSPARENCY = yes
ENABLE_UNITY = yes
ENABLE_XFWM = yes
GNOME_SHELL_FOUND = 
GNOME_SHELL_VERSION = 
GTK3_VERSION = 3.24
INKSCAPE = /usr/bin/inkscape
INSTALL = /usr/bin/install -c
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_PROGRAM = ${INSTALL}
INSTALL_SCRIPT = ${INSTALL}
INSTALL_STRIP_PROGRAM = $(install_sh) -c -s
LIBOBJS = 
LIBS = 
LN_S = ln -s
LTLIBOBJS = 
MAKEINFO = ${SHELL} /home/bryan/arc-theme/build-aux/missing makeinfo
MKDIR_P = /usr/bin/mkdir -p
OPTIPNG = 
PACKAGE = arc-theme
PACKAGE_BUGREPORT = https://github.com/jnsh/arc-theme/issues
PACKAGE_NAME = arc-theme
PACKAGE_STRING = arc-theme 20200819
PACKAGE_TARNAME = arc-theme
PACKAGE_URL = https://github.com/jnsh/arc-theme
PACKAGE_VERSION = 20200819
PATH_SEPARATOR = :
PKG_CONFIG = /usr/bin/pkg-config
PKG_CONFIG_LIBDIR = 
PKG_CONFIG_PATH = 
SASSC = /usr/bin/sassc
SET_MAKE = 
SHELL = /bin/sh
STRIP = 
VERSION = 20200819
abs_builddir = /home/bryan/arc-theme/common/gtk-2.0
abs_srcdir = /home/bryan/arc-theme/common/gtk-2.0
abs_top_builddir = /home/bryan/arc-theme
abs_top_srcdir = /home/bryan/arc-theme
am__leading_dot = .
am__tar = $${TAR-tar} chof - "$$tardir"
am__untar = $${TAR-tar} xf -
bindir = ${exec_prefix}/bin
build_alias = 
builddir = .
datadir = ${datarootdir}
datarootdir = ${prefix}/share
docdir = ${datarootdir}/doc/${PACKAGE_TARNAME}
dvidir = ${docdir}
exec_prefix = ${prefix}
gtk3themedarkdir = ${datarootdir}/themes/Arc-Dark/gtk-3.0
gtk3themedarkerdir = ${datarootdir}/themes/Arc-Darker/gtk-3.0
gtk3themedir = ${datarootdir}/themes/Arc/gtk-3.0
gtk3themelighterdir = ${datarootdir}/themes/Arc-Lighter/gtk-3.0
host_alias = 
htmldir = ${docdir}
includedir = ${prefix}/include
infodir = ${datarootdir}/info
install_sh = ${SHELL} /home/bryan/arc-theme/build-aux/install-sh
libdir = ${exec_prefix}/lib
libexecdir = ${exec_prefix}/libexec
localedir = ${datarootdir}/locale
localstatedir = ${prefix}/var
mandir = ${datarootdir}/man
mkdir_p = $(MKDIR_P)
oldincludedir = /usr/include
pdfdir = ${docdir}
prefix = /usr
program_transform_name = s,x,x,
psdir = ${docdir}
sbindir = ${exec_prefix}/sbin
sharedstatedir = ${prefix}/com
srcdir = .
sysconfdir = ${prefix}/etc
target_alias = 
themedarkdir = ${datarootdir}/themes/Arc-Dark
themedarkerdir = ${datarootdir}/themes/Arc-Darker
themedir = ${datarootdir}/themes/Arc
themelighterdir = ${datarootdir}/themes/Arc-Lighter
top_build_prefix = ../../
top_builddir = ../..
top_srcdir = ../..
targets := $(shell cat $(srcdir)/assets.txt)
light := $(patsubst %,light/assets/%.png,$(targets))
dark := $(patsubst %,dark/assets/%.png,$(targets))
mbt_entry := $(filter entry-%toolbar,$(targets))
mbt_menubar := $(filter menubar%,$(targets))
mbt_button := $(filter button%,$(targets))
mbt_light := \
	$(srcdir)/menubar-toolbar/menubar-toolbar.rc \
	$(patsubst %,menubar-toolbar/%.png,$(mbt_entry) $(mbt_menubar))

mbt_dark := \
	$(srcdir)/menubar-toolbar/menubar-toolbar-dark.rc \
	$(patsubst %,menubar-toolbar/%-dark.png,$(mbt_entry) $(mbt_menubar)) \
	$(patsubst %,menubar-toolbar/%.png,$(mbt_button))

rc := apps main panel xfce-notify
srcrc := $(patsubst %,$(srcdir)/%.rc,$(rc))
gtk2themedir = $(themedir)/gtk-2.0
gtk2themeassetsdir = $(gtk2themedir)/assets
gtk2theme_DATA = $(srcrc) $(srcdir)/light/gtkrc
gtk2themeassets_DATA = $(light)
nobase_gtk2theme_DATA = $(mbt_light)
gtk2themedarkerdir = $(themedarkerdir)/gtk-2.0
gtk2themedarkerassetsdir = $(gtk2themedarkerdir)/assets
gtk2themedarker_DATA = $(srcrc) $(srcdir)/darker/gtkrc
gtk2themedarkerassets_DATA = $(light)
nobase_gtk2themedarker_DATA = $(mbt_dark)
gtk2themedarkdir = $(themedarkdir)/gtk-2.0
gtk2themedarkassetsdir = $(gtk2themedarkdir)/assets
gtk2themedark_DATA = $(srcrc) $(srcdir)/dark/gtkrc
gtk2themedarkassets_DATA = $(dark)
nobase_gtk2themedark_DATA = $(mbt_dark)
gtk2themelighterdir = $(themelighterdir)/gtk-2.0
gtk2themelighterassetsdir = $(gtk2themelighterdir)/assets
gtk2themelighter_DATA = $(srcrc) $(srcdir)/lighter/gtkrc
gtk2themelighterassets_DATA = $(light)
nobase_gtk2themelighter_DATA = $(mbt_light)
EXTRA_DIST = \
	assets.txt \
	$(srcrc) \
	$(patsubst %,%/assets.svg,light dark) \
	$(patsubst %,%/gtkrc,light darker dark lighter) \
	$(patsubst %,menubar-toolbar/menubar-toolbar%,.rc -dark.rc)

all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign common/gtk-2.0/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign common/gtk-2.0/Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__maybe_remake_depfiles)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__maybe_remake_depfiles);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):
install-gtk2themeDATA: $(gtk2theme_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2theme_DATA)'; test -n "$(gtk2themedir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themedir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themedir)" || exit $$?; \
	done

uninstall-gtk2themeDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2theme_DATA)'; test -n "$(gtk2themedir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themedir)'; $(am__uninstall_files_from_dir)
install-gtk2themeassetsDATA: $(gtk2themeassets_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themeassets_DATA)'; test -n "$(gtk2themeassetsdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themeassetsdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themeassetsdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themeassetsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themeassetsdir)" || exit $$?; \
	done

uninstall-gtk2themeassetsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themeassets_DATA)'; test -n "$(gtk2themeassetsdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themeassetsdir)'; $(am__uninstall_files_from_dir)
install-gtk2themedarkDATA: $(gtk2themedark_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themedark_DATA)'; test -n "$(gtk2themedarkdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themedarkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themedarkdir)" || exit $$?; \
	done

uninstall-gtk2themedarkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themedark_DATA)'; test -n "$(gtk2themedarkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themedarkdir)'; $(am__uninstall_files_from_dir)
install-gtk2themedarkassetsDATA: $(gtk2themedarkassets_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themedarkassets_DATA)'; test -n "$(gtk2themedarkassetsdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkassetsdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkassetsdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themedarkassetsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themedarkassetsdir)" || exit $$?; \
	done

uninstall-gtk2themedarkassetsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themedarkassets_DATA)'; test -n "$(gtk2themedarkassetsdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themedarkassetsdir)'; $(am__uninstall_files_from_dir)
install-gtk2themedarkerDATA: $(gtk2themedarker_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themedarker_DATA)'; test -n "$(gtk2themedarkerdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkerdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkerdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themedarkerdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themedarkerdir)" || exit $$?; \
	done

uninstall-gtk2themedarkerDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themedarker_DATA)'; test -n "$(gtk2themedarkerdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themedarkerdir)'; $(am__uninstall_files_from_dir)
install-gtk2themedarkerassetsDATA: $(gtk2themedarkerassets_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themedarkerassets_DATA)'; test -n "$(gtk2themedarkerassetsdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkerassetsdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkerassetsdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themedarkerassetsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themedarkerassetsdir)" || exit $$?; \
	done

uninstall-gtk2themedarkerassetsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themedarkerassets_DATA)'; test -n "$(gtk2themedarkerassetsdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themedarkerassetsdir)'; $(am__uninstall_files_from_dir)
install-gtk2themelighterDATA: $(gtk2themelighter_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themelighter_DATA)'; test -n "$(gtk2themelighterdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themelighterdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themelighterdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themelighterdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themelighterdir)" || exit $$?; \
	done

uninstall-gtk2themelighterDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themelighter_DATA)'; test -n "$(gtk2themelighterdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themelighterdir)'; $(am__uninstall_files_from_dir)
install-gtk2themelighterassetsDATA: $(gtk2themelighterassets_DATA)
	@$(NORMAL_INSTALL)
	@list='$(gtk2themelighterassets_DATA)'; test -n "$(gtk2themelighterassetsdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themelighterassetsdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themelighterassetsdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gtk2themelighterassetsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gtk2themelighterassetsdir)" || exit $$?; \
	done

uninstall-gtk2themelighterassetsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gtk2themelighterassets_DATA)'; test -n "$(gtk2themelighterassetsdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gtk2themelighterassetsdir)'; $(am__uninstall_files_from_dir)
install-nobase_gtk2themeDATA: $(nobase_gtk2theme_DATA)
	@$(NORMAL_INSTALL)
	@list='$(nobase_gtk2theme_DATA)'; test -n "$(gtk2themedir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedir)" || exit 1; \
	fi; \
	$(am__nobase_list) | while read dir files; do \
	  xfiles=; for file in $$files; do \
	    if test -f "$$file"; then xfiles="$$xfiles $$file"; \
	    else xfiles="$$xfiles $(srcdir)/$$file"; fi; done; \
	  test -z "$$xfiles" || { \
	    test "x$$dir" = x. || { \
	      echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedir)/$$dir'"; \
	      $(MKDIR_P) "$(DESTDIR)$(gtk2themedir)/$$dir"; }; \
	    echo " $(INSTALL_DATA) $$xfiles '$(DESTDIR)$(gtk2themedir)/$$dir'"; \
	    $(INSTALL_DATA) $$xfiles "$(DESTDIR)$(gtk2themedir)/$$dir" || exit $$?; }; \
	done

uninstall-nobase_gtk2themeDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(nobase_gtk2theme_DATA)'; test -n "$(gtk2themedir)" || list=; \
	$(am__nobase_strip_setup); files=`$(am__nobase_strip)`; \
	dir='$(DESTDIR)$(gtk2themedir)'; $(am__uninstall_files_from_dir)
install-nobase_gtk2themedarkDATA: $(nobase_gtk2themedark_DATA)
	@$(NORMAL_INSTALL)
	@list='$(nobase_gtk2themedark_DATA)'; test -n "$(gtk2themedarkdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkdir)" || exit 1; \
	fi; \
	$(am__nobase_list) | while read dir files; do \
	  xfiles=; for file in $$files; do \
	    if test -f "$$file"; then xfiles="$$xfiles $$file"; \
	    else xfiles="$$xfiles $(srcdir)/$$file"; fi; done; \
	  test -z "$$xfiles" || { \
	    test "x$$dir" = x. || { \
	      echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkdir)/$$dir'"; \
	      $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkdir)/$$dir"; }; \
	    echo " $(INSTALL_DATA) $$xfiles '$(DESTDIR)$(gtk2themedarkdir)/$$dir'"; \
	    $(INSTALL_DATA) $$xfiles "$(DESTDIR)$(gtk2themedarkdir)/$$dir" || exit $$?; }; \
	done

uninstall-nobase_gtk2themedarkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(nobase_gtk2themedark_DATA)'; test -n "$(gtk2themedarkdir)" || list=; \
	$(am__nobase_strip_setup); files=`$(am__nobase_strip)`; \
	dir='$(DESTDIR)$(gtk2themedarkdir)'; $(am__uninstall_files_from_dir)
install-nobase_gtk2themedarkerDATA: $(nobase_gtk2themedarker_DATA)
	@$(NORMAL_INSTALL)
	@list='$(nobase_gtk2themedarker_DATA)'; test -n "$(gtk2themedarkerdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkerdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkerdir)" || exit 1; \
	fi; \
	$(am__nobase_list) | while read dir files; do \
	  xfiles=; for file in $$files; do \
	    if test -f "$$file"; then xfiles="$$xfiles $$file"; \
	    else xfiles="$$xfiles $(srcdir)/$$file"; fi; done; \
	  test -z "$$xfiles" || { \
	    test "x$$dir" = x. || { \
	      echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themedarkerdir)/$$dir'"; \
	      $(MKDIR_P) "$(DESTDIR)$(gtk2themedarkerdir)/$$dir"; }; \
	    echo " $(INSTALL_DATA) $$xfiles '$(DESTDIR)$(gtk2themedarkerdir)/$$dir'"; \
	    $(INSTALL_DATA) $$xfiles "$(DESTDIR)$(gtk2themedarkerdir)/$$dir" || exit $$?; }; \
	done

uninstall-nobase_gtk2themedarkerDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(nobase_gtk2themedarker_DATA)'; test -n "$(gtk2themedarkerdir)" || list=; \
	$(am__nobase_strip_setup); files=`$(am__nobase_strip)`; \
	dir='$(DESTDIR)$(gtk2themedarkerdir)'; $(am__uninstall_files_from_dir)
install-nobase_gtk2themelighterDATA: $(nobase_gtk2themelighter_DATA)
	@$(NORMAL_INSTALL)
	@list='$(nobase_gtk2themelighter_DATA)'; test -n "$(gtk2themelighterdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themelighterdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(gtk2themelighterdir)" || exit 1; \
	fi; \
	$(am__nobase_list) | while read dir files; do \
	  xfiles=; for file in $$files; do \
	    if test -f "$$file"; then xfiles="$$xfiles $$file"; \
	    else xfiles="$$xfiles $(srcdir)/$$file"; fi; done; \
	  test -z "$$xfiles" || { \
	    test "x$$dir" = x. || { \
	      echo " $(MKDIR_P) '$(DESTDIR)$(gtk2themelighterdir)/$$dir'"; \
	      $(MKDIR_P) "$(DESTDIR)$(gtk2themelighterdir)/$$dir"; }; \
	    echo " $(INSTALL_DATA) $$xfiles '$(DESTDIR)$(gtk2themelighterdir)/$$dir'"; \
	    $(INSTALL_DATA) $$xfiles "$(DESTDIR)$(gtk2themelighterdir)/$$dir" || exit $$?; }; \
	done

uninstall-nobase_gtk2themelighterDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(nobase_gtk2themelighter_DATA)'; test -n "$(gtk2themelighterdir)" || list=; \
	$(am__nobase_strip_setup); files=`$(am__nobase_strip)`; \
	dir='$(DESTDIR)$(gtk2themelighterdir)'; $(am__uninstall_files_from_dir)
tags TAGS:

ctags CTAGS:

cscope cscopelist:


distdir: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) distdir-am

distdir-am: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(gtk2themedir)" "$(DESTDIR)$(gtk2themeassetsdir)" "$(DESTDIR)$(gtk2themedarkdir)" "$(DESTDIR)$(gtk2themedarkassetsdir)" "$(DESTDIR)$(gtk2themedarkerdir)" "$(DESTDIR)$(gtk2themedarkerassetsdir)" "$(DESTDIR)$(gtk2themelighterdir)" "$(DESTDIR)$(gtk2themelighterassetsdir)" "$(DESTDIR)$(gtk2themedir)" "$(DESTDIR)$(gtk2themedarkdir)" "$(DESTDIR)$(gtk2themedarkerdir)" "$(DESTDIR)$(gtk2themelighterdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-gtk2themeDATA install-gtk2themeassetsDATA \
	install-gtk2themedarkDATA install-gtk2themedarkassetsDATA \
	install-gtk2themedarkerDATA install-gtk2themedarkerassetsDATA \
	install-gtk2themelighterDATA \
	install-gtk2themelighterassetsDATA \
	install-nobase_gtk2themeDATA install-nobase_gtk2themedarkDATA \
	install-nobase_gtk2themedarkerDATA \
	install-nobase_gtk2themelighterDATA

install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-gtk2themeDATA uninstall-gtk2themeassetsDATA \
	uninstall-gtk2themedarkDATA uninstall-gtk2themedarkassetsDATA \
	uninstall-gtk2themedarkerDATA \
	uninstall-gtk2themedarkerassetsDATA \
	uninstall-gtk2themelighterDATA \
	uninstall-gtk2themelighterassetsDATA \
	uninstall-nobase_gtk2themeDATA \
	uninstall-nobase_gtk2themedarkDATA \
	uninstall-nobase_gtk2themedarkerDATA \
	uninstall-nobase_gtk2themelighterDATA

.MAKE: install-am install-strip

.PHONY: all all-am check check-am clean clean-generic cscopelist-am \
	ctags-am distclean distclean-generic distdir dvi dvi-am html \
	html-am info info-am install install-am install-data \
	install-data-am install-dvi install-dvi-am install-exec \
	install-exec-am install-gtk2themeDATA \
	install-gtk2themeassetsDATA install-gtk2themedarkDATA \
	install-gtk2themedarkassetsDATA install-gtk2themedarkerDATA \
	install-gtk2themedarkerassetsDATA install-gtk2themelighterDATA \
	install-gtk2themelighterassetsDATA install-html \
	install-html-am install-info install-info-am install-man \
	install-nobase_gtk2themeDATA install-nobase_gtk2themedarkDATA \
	install-nobase_gtk2themedarkerDATA \
	install-nobase_gtk2themelighterDATA install-pdf install-pdf-am \
	install-ps install-ps-am install-strip installcheck \
	installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic pdf \
	pdf-am ps ps-am tags-am uninstall uninstall-am \
	uninstall-gtk2themeDATA uninstall-gtk2themeassetsDATA \
	uninstall-gtk2themedarkDATA uninstall-gtk2themedarkassetsDATA \
	uninstall-gtk2themedarkerDATA \
	uninstall-gtk2themedarkerassetsDATA \
	uninstall-gtk2themelighterDATA \
	uninstall-gtk2themelighterassetsDATA \
	uninstall-nobase_gtk2themeDATA \
	uninstall-nobase_gtk2themedarkDATA \
	uninstall-nobase_gtk2themedarkerDATA \
	uninstall-nobase_gtk2themelighterDATA

.PRECIOUS: Makefile


light: $(light)
dark: $(dark)
mbt_light: $(mbt_light)
mbt_dark: $(mbt_dark)

light/assets dark/assets menubar-toolbar:
	$(MKDIR_P) "$@"/

clean:
	rm -rf light/assets/ dark/assets/ menubar-toolbar/*.png

$(light): $(srcdir)/light/assets.svg | light/assets
$(dark): $(srcdir)/dark/assets.svg | dark/assets

$(light) $(dark):
	$(INKSCAPE) --export-id-only --export-filename="$@" --export-id="$(basename $(notdir $@))" --export-dpi=96 "$<" >/dev/null
#	$(INKSCAPE) --export-id-only --export-png="$@" --export-id="$(basename $(notdir $@))" --export-dpi=96 "$<" >/dev/null
#	$(OPTIPNG) -o7 --quiet "$@"

menubar-toolbar/%-dark.png: dark/assets/%.png | menubar-toolbar
	cp "$<" "$@"

menubar-toolbar/%.png: light/assets/%.png | menubar-toolbar
	cp "$<" "$@"

.PHONY: light dark mbt_light mbt_dark clean

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
